using AllNotes.Models;
using System;
using System.Collections.Generic;
using System.Text;

namespace AllNotes.Services
{
    public class FolderService
    {

        /* private static FolderService _instance;
         public static FolderService Instance => _instance ?? (_instance = new FolderService());

         private List<AppFolder> _folders; // Or use ObservableCollection<AppFolder>

         public FolderService()
         {
             _folders = new List<AppFolder>(); // Initialize with current folder data
         }

         public IEnumerable<AppFolder> GetFolders()
         {
             return _folders;
         }

         public void UpdateFolder(AppFolder folder)
         {
             // Update folder logic
             OnFolderUpdated?.Invoke(this, EventArgs.Empty);
         }

         public event EventHandler OnFolderUpdated;
     }*/
    }
}