<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
             x:Class="AllNotes.Views.CustomToolbar">
    <ScrollView Orientation="Horizontal" HorizontalScrollBarVisibility="Never">
        <StackLayout Orientation="Horizontal" BackgroundColor="LightGray" Padding="5">
            <Button Text="B" Command="{Binding BoldTextCommand}" Margin="2"/>
            <Button Text="I" Command="{Binding ItalicCommand}" Margin="2"/>
            <Button Text="U" Command="{Binding UnderlineCommand}" Margin="2"/>
         <!--   <Button Text="Edit in TEditor" Command="{Binding OpenTEditorCommand}" />-->

            <!-- Add other buttons as needed, with Margin for spacing -->
        </StackLayout>
    </ScrollView>
</ContentView>