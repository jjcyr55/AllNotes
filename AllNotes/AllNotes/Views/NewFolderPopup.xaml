<?xml version="1.0" encoding="utf-8" ?>
<xct:Popup xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
             x:Class="AllNotes.Views.NewFolderPopup"
             x:Name="Popup2"
             Size="300,500"
            
             BackgroundColor="Transparent"
             xct:ShadowEffect.Color="Transparent">

    <StackLayout>
        <Entry Placeholder="Enter folder name" Text="{Binding NewFolderName}" />
        <Button Text="OK" Command="{Binding OkCommand}" />
        <Button Text="Cancel" Command="{Binding CancelCommand}" />
        
    </StackLayout>
</xct:Popup>
<!--<Button Text="Cancel" Clicked="Button_Clicked" />-->